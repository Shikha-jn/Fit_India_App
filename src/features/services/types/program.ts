export interface RecoveryBlueprint {
      id: string;
      icon: string;
      tag: string;
      title: string;
      description: string;
      checklist: string[];
}

export interface HealthIssue {
      label: string;
}